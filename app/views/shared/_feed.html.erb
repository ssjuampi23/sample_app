<% if @feed_items.any? %> <!--the partial communicates directly with the controller and have access directly to the variables declared on the controller -->
	<ol class="microposts">
		<%= render partial: 'shared/feed_item', collection: @feed_items %>
	</ol>
	<%= will_paginate @feed_items %>
<% end %>
